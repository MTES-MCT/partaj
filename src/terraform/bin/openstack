#!/usr/bin/env bash

set -eo pipefail

# Run OpenStack commands in the docker container passing our environment variables

docker run --rm -it \
    -u "$(id -u)" \
    -v "${PWD}/:/app" \
    -w "/app" \
    --env-file ./env.d/development \
    "openstack-cli:latest" \
    "$@"
